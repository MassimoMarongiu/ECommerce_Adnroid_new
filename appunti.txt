https://www.youtube.com/watch?v=CQZ8_OH7zwI&list=PLraJmOvF9eeGyVClKdBaVU6O_XqQjNboS&index=48
44:33

da fare
login register
https://www.youtube.com/watch?v=lpiBuNQDkPs&list=PLraJmOvF9eeGyVClKdBaVU6O_XqQjNboS&index=24

branches github
Crea un nuovo branch:
  nome_del_nuovo_branch
git checkout -b optimized_recyclerview_and_fixed_soma_issues

spostarti sul branch di base (ad esempio main o develop):
git checkout master


Verifica che il nuovo branch sia stato creato e selezionato:
git branch
Dovresti vedere un asterisco (*) accanto al nome del nuovo branch.

Committa le modifiche:
Crea un commit con un messaggio descrittivo:
git commit -m "Descrizione delle modifiche"

Aggiungi i file modificati al commit:
git add .

Pushare il branch:
git push -u origin nome_del_nuovo_branch


toolbar presa da myorders_activity

immagini circolari aggiungere in build gradle kts     implementation("de.hdodenhof:circleimageview:3.1.0")


differenze activity/fragment/adapter
activity:
addToWishlistBtn = findViewById(R.id.add_to_wishlist_btn);


Intent deliveryIntent= new Intent(ProductDetailsActivity.this,DeliveryActivity.class);
startActivity(deliveryIntent);

fragment:
Intent deliveryIntent = new Intent(getContext(),DeliveryActivity.class);
getContext().startActivity(deliveryIntent);

adapter:
categoryIcon = itemView.findViewById(R.id.category_icon);

Intent categoryIntent = new Intent(itemView.getContext(), CategoryActivity.class);
itemView.getContext().startActivity(categoryIntent);